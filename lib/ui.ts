/**
 * Shared class tokens for patterns that repeat across pages. Kept as strings
 * rather than wrapper components so the markup stays readable. The classes
 * themselves live in `app/globals.css` and mirror the editor QSS.
 */

/** Inline code inside prose, definition lists and tables. */
export const INLINE_CODE = "aph-key";

/** Descriptive inline link. */
export const TEXT_LINK = "aph-link";
